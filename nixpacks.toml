[phases.setup]
nixPkgs = ["php84", "php84Extensions.exif", "php84Extensions.pdo_mysql", "nodejs_22"]

[phases.install]
cmds = [
  "composer install --no-interaction --prefer-dist --optimize-autoloader",
  "npm ci",
  "npm run build"
]

[start]
cmd = "php artisan serve --host=0.0.0.0 --port=$PORT"
